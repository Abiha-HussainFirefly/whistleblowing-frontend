import { Link } from 'react-router-dom';
import { Button } from '@components/ui/button';
import { BrandLogo } from '@components/common/BrandLogo';
import { ROUTES } from '@config/routes';
import type { ReactElement } from 'react';

export function LandingPage(): ReactElement {
  const loggedIn = Boolean(localStorage.getItem('wb.internalToken'));
  const destination = loggedIn ? ROUTES.WHISTLEBLOWING : ROUTES.AUTH.LOGIN;
  return <div className="min-h-screen bg-brand-primary text-white"><header className="mx-auto flex max-w-7xl items-center justify-between px-6 py-5"><Link to={ROUTES.ROOT}><BrandLogo white className="h-10 w-auto" /></Link><nav className="hidden items-center gap-8 text-sm text-slate-300 md:flex"><a href="#features" className="hover:text-white">Features</a><a href="#platform" className="hover:text-white">Platform</a><a href="#contact" className="hover:text-white">Contact</a></nav><Button asChild><Link to={destination}>{loggedIn ? 'Go to Dashboard' : 'Sign In'}</Link></Button></header><section className="relative overflow-hidden px-6 py-24 md:py-32" style={{ backgroundImage: 'radial-gradient(circle at 20% 30%, rgba(255,255,255,0.06), transparent 40%), radial-gradient(circle at 80% 70%, rgba(67,160,71,0.12), transparent 45%)' }}><div className="mx-auto max-w-4xl text-center"><span className="inline-block rounded-full border border-white/20 px-4 py-1 text-xs uppercase tracking-wider text-slate-300">Legal Operations · Powered by AI</span><h1 className="mt-6 text-4xl font-semibold leading-tight md:text-6xl">Centralize every contract, case, and compliance record.</h1><p className="mx-auto mt-6 max-w-2xl text-lg leading-relaxed text-slate-300">Civorah unifies legal records, contract lifecycle, litigation tracking, and compliance for enterprise legal departments — secured by default and accelerated by AI.</p><div className="mt-10"><Button asChild size="lg"><Link to={destination}>{loggedIn ? 'Go to Dashboard' : 'Sign In'}</Link></Button></div></div></section><section id="features" className="border-t border-white/10 bg-[#032147] px-6 py-20"><div className="mx-auto max-w-6xl"><h2 className="text-3xl font-semibold">Built for enterprise legal teams</h2><p className="mt-3 max-w-2xl text-slate-300">Replace fragmented spreadsheets, shared drives, and email threads with one secure source of truth.</p><div className="mt-12 grid gap-6 sm:grid-cols-2 lg:grid-cols-4">{[['Contract Lifecycle','Draft, review, sign, and renew — every clause version traceable end to end.'],['Matters Tracking','Case files, hearings, deadlines, and outside-counsel spend in a single view.'],['Compliance','Policies, attestations, and audit trails aligned to your regulatory regime.'],['AI Search','Natural-language search across every document, ruling, and clause in your archive.']].map(([title, body]) => <article key={title} className="rounded-xl border border-white/10 bg-white/5 p-6 backdrop-blur-sm"><h3 className="text-lg font-semibold text-white">{title}</h3><p className="mt-2 text-sm leading-relaxed text-slate-300">{body}</p></article>)}</div></div></section><footer id="contact" className="border-t border-white/10 px-6 py-10 text-xs text-slate-400"><div className="mx-auto flex max-w-6xl flex-col items-center justify-between gap-3 md:flex-row"><p>© {new Date().getFullYear()} Civorah. All rights reserved.</p><Link to={ROUTES.AUTH.ADMIN_LOGIN} className="hover:text-white">System administrator sign in</Link></div></footer></div>;
}
